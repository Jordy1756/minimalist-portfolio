---
export const prerender = false;

const browserLang = Astro.request.headers.get("accept-language");
const preferredLang = browserLang?.startsWith("en") ? "en" : "es";

return Astro.redirect(`/${preferredLang}/`, 302);
---
