---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {}

const { target = "_blank", rel = "noopener noreferrer", ...props } = Astro.props;
---

<a target={target} rel={rel} {...props}>
	<slot />
</a>
