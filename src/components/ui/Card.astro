---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<article class:list={["flex flex-col gap-1 rounded-lg border border-neutral-200 p-4", className]}>
	<slot />
</article>
