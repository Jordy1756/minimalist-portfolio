---
import TimePeriod from '@components/ui/TimePeriod.astro';
import { getCurrentLocale } from '@i18n/utils';

interface Props {
  institution: string;
  area: string;
  startDate: Date | null;
  endDate: Date | null;
}

const { institution, area, startDate, endDate } = Astro.props;

const currentLocale = getCurrentLocale(Astro.currentLocale);

const localeCode = currentLocale === 'es' ? 'es-ES' : 'en-US';
const presentText = currentLocale === 'es' ? 'Presente' : 'Present';
---

<article>
  <header>
    <header class="flex justify-between gap-2">
      <div class="flex flex-col">
        <h3 class="text-lg font-semibold text-neutral-800">{institution}</h3>
      </div>
      <TimePeriod startDate={startDate} endDate={endDate} localeCode={localeCode} presentText={presentText} />
    </header>
  </header>
  <section>
    <p>{area}</p>
  </section>
</article>
